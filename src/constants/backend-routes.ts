export enum BackendRoutes {
  LOGIN = "/auth/login",
  USERS = "/users",
  USER_DETAILS = "/admin/users/user-details",
  ANIMALS = "/dogs",
  SPONSORSHIP = "/sponsorship",
  DONATIONS = "/donations",
}
