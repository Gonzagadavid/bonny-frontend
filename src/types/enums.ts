export enum CandidacyStatus {
  PENDING = "PENDING",
  REJECTED = "REJECTED",
  CANCELED = "CANCELED",
  APPROVED = "APPROVED",
  CONCLUDED = "CONCLUDED",
}

export enum DogSize {
  BIG = "BIG",
  MEDIUM = "MEDIUM",
  SMALL = "SMALL",
}

export enum DogFell {
  SHORT = "SHORT",
  LONG = "LONG",
}

export enum GenderEnum {
  MALE = "MALE",
  FEMALE = "FEMALE",
}

export enum UserRole {
  USER = "USER",
  ADMIN = "ADMIN",
  VOLUNTEER = "VOLUNTEER",
}
